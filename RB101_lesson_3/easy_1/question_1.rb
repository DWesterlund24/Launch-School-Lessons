# [1, 2, 2, 3]
# The output isn't changed by #uniq because #uniq doesn not mutate its caller
# If it was #uniq! then [1, 2, 3] would be the output

# The arrays are actually shown each on a new line because we are using puts and not print or p

print [1, 2, 3]